part of '../home.dart';

class LeaderCardWidget extends StatelessWidget {
  const LeaderCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
